# 云端迁移方案 · 热点追踪助手

> 目标一句话：**本地电脑周一没开机，周报也照发不误。**

---

## 一、现状梳理（迁移前的完整盘点）

### 1.1 现有「代码」的真实形态

先说清楚一个关键事实：**迁移前项目里没有任何可执行的抓取代码。**
整套流程存在于一份自然语言 prompt 中（自动化 `automation-1788517206707`），
由 WorkBuddy 桌面应用内的 LLM 运行时解释执行：

| 环节 | 原实现方式 | 依赖 |
|---|---|---|
| 定时调度 | `LocalAutomationScheduler`，RRULE `FREQ=WEEKLY;BYDAY=MO;BYHOUR=14;BYMINUTE=0` | **WorkBuddy 桌面应用进程常驻** |
| 热点数据源 | arXiv（WebSearch/WebFetch 工具实时检索） | LLM 运行时 |
| 筛选规则 | prompt 第一段：近 12 个月为主、重点近一周新增、5-10 篇 | LLM 判断 |
| 链接校验 | prompt 第二段：逐条打开核对、会议归属只认 comments 字段 | LLM + WebFetch |
| 邮件发送 | `mcp__qq-mail`（9/4 成功过一次，现已下线）→ `mcp__agent-mail` | WorkBuddy MCP 连接器 |
| 去重 | `.workbuddy/papers_seen.json`（arXiv ID 列表） | 本地文件 |
| 收件人 | 1586133556@qq.com（写在 prompt 里，硬编码） | — |

### 1.2 迁移必须解决的四个问题

1. **调度依赖桌面进程** —— 2026-09-07 未送达事故的根因：那天应用没启动，
   任务没触发，12 小时补发窗口过后永久丢失。这是迁云的核心动机。
2. **无可执行代码** —— prompt 形态无法搬到云端，必须下沉为 Python。
3. **凭据硬编码** —— 收件人邮箱直接写在 prompt 里；迁云后所有凭据必须走环境变量。
4. **无失败兜底** —— 原流程发送失败即静默丢失，无日志无告警无重试。

### 1.3 迁移后的代码结构

```
热点追踪助手/
├── run.py                    # CLI 入口，本地/云端共用
├── tracker/
│   ├── config.py             # 环境变量 + .env 配置（脱敏日志）
│   ├── arxiv_client.py       # arXiv Atom API：抓取+重试+代码链接提取
│   ├── llm_client.py         # LLM 调用（OpenAI 兼容协议，零依赖）
│   ├── prompts.py            # 三段式 prompt 下沉的系统提示词
│   ├── report.py             # 邮件正文渲染（HTML+纯文本双备选）
│   ├── mailer.py             # SMTP 发送（SSL/STARTTLS+重试）
│   ├── state.py              # papers_seen.json 读写 + ISO 周幂等
│   └── pipeline.py           # 全链路编排 + 退出码约定
├── .github/workflows/weekly-papers.yml   # 云端定时调度
├── .env.example              # 环境变量样例（真实 .env 不入库）
├── .gitignore                # 排除 .env / reports / papers_seen.json
├── requirements.txt          # 零依赖声明
└── test_pipeline_mock.py     # 端到端 mock 测试（已验证通过）
```

**防幻觉架构**（本次迁移最重要的设计决策）：
论文标题、arXiv ID、链接、会议信息——全部由 `arxiv_client.py` 从真实
抓取结果注入，LLM 只负责「挑选」和「写作」，提示词里明确禁止改写任何
字段、禁止补充候选池之外的论文。`pipeline.py` 里还有一道代码级闸门：
LLM 返回的 arxiv_id 若不在候选池中直接丢弃（mock 测试已验证此路径）。

---

## 二、云端方案选型

### 2.1 对比（按你的场景排序）

| 方案 | 定时可靠性 | 成本 | 运维负担 | 结论 |
|---|---|---|---|---|
| **GitHub Actions（选用）** | cron 由 GitHub 云端触发，**与本地开机无关** | 公共仓库免费；私有仓库 2000 分钟/月免费额度，本任务月耗约 20 分钟 | 最低：代码即仓库，Secrets 界面化管理，失败自动邮件通知 | ✅ **推荐** |
| 云函数 + 云触发器（腾讯云 SCF / 阿里云 FC） | 高 | 免费额度通常够用（SCF 每月 100 万次调用），但**冷启动慢、层依赖上传麻烦** | 中：需维护函数包、层、触发器、日志主题多套控制台配置 | 备选 |
| 云服务器 crontab | 最高（完全自主） | 轻量服务器约 ¥50-100/月 | 高：为每周 5 分钟的任务养一台机器，明显浪费 | 不推荐 |
| 继续用本地 automation | **低**（依赖开机，已被事故证伪） | 免费 | — | 仅作本地兜底保留 |

**选择理由**：项目本身要进 Git 仓库；Actions 与仓库天然一体，代码、
调度、Secrets、失败告警、运行日志全在一个地方。且 `workflow_dispatch`
支持网页一键手动触发，部署当天就能验证，不用等下周一。

### 2.2 成本

- **公共仓库**：Actions 完全免费，无额度概念。
- **私有仓库**：免费 2000 分钟/月。本流水线单次 3-5 分钟，
  每月 4 次定时 + 若干次手动 ≈ 30 分钟，**占额度的 1.5%**。
- DeepSeek API：单次约 2 万 token 输入 + 2 千输出，按官价约 **¥0.03/次**，月耗 ¥0.1 级别。

---

## 三、触发时间与语义保持

| 项 | 原值 | 迁移后 | 说明 |
|---|---|---|---|
| 触发时刻 | 周一 08:00（本地） | 周一 **14:00 CST** = cron `0 6 * * 1` UTC | 2026-09-09 应用户要求从 08:00 调到 14:00，云端沿用此值 |
| 频率 | 每周一次 | 每周一次 | 不变 |
| 筛选规则 | 近 12 个月为主、重点近一周新增、5-10 篇、禁止凑数 | `LOOKBACK_DAYS=30` + `MIN_PICKS=5`/`MAX_PICKS=10`，prompt 明示「挑不满 5 篇就少推」 | 语义保持 |
| 正文格式 | 速览表 + 前沿理由 + 趋势判断 + 校验记录 | `report.py` 四段结构逐段对应 | 不变 |
| 去重 | papers_seen.json，推过的 ID 永不再推 | 同一文件 + 新增 ISO 周幂等键 | 增强（防重发） |
| 无新增处理 | 发一封「本周无新增」说明信，不凑数 | `pipeline.py` 同分支实现 | 不变 |

时区说明：Actions cron 是 UTC 且**不随 DST 变化**，`0 6 * * 1` 恒等于
北京时间周一 14:00。本地 automation 的 RRULE 无 TZID、依赖客户端时区，
迁云后这一不确定性自然消除。

---

## 四、敏感信息管理

**原则：凭据只存在于两个地方 —— 本地 `.env`（已 gitignore）或 GitHub
Secrets（加密存储、日志自动脱敏）。代码和 workflow 文件里只有变量名。**

需要配置的 4 个 Secrets（Settings → Secrets and variables → Actions）：

| Secret 名 | 内容 | 获取方式 |
|---|---|---|
| `SMTP_USER` | 你的 QQ 邮箱地址 | — |
| `SMTP_AUTH_CODE` | SMTP 授权码（**不是登录密码**） | 网页版 QQ 邮箱 → 设置 → 账号 → 开启 SMTP 服务 → 生成授权码 |
| `MAIL_TO` | 收件人 1586133556@qq.com | — |
| `LLM_API_KEY` | DeepSeek API Key | platform.deepseek.com 控制台 |

非敏感配置（领域名、检索参数）直接写在 workflow 的 `env:` 段，改起来
直观。换 LLM 供应商只需改 `LLM_BASE_URL` + `LLM_MODEL` 两个环境变量。

---

## 五、可靠性设计（重试 / 幂等 / 告警 / 防丢失）

### 5.1 重试（三层，均为指数退避）

| 层 | 位置 | 策略 |
|---|---|---|
| arXiv 抓取 | `arxiv_client.py` | 3 次，遇 4xx（请求本身错）立即放弃 |
| LLM 调用 | `llm_client.py` | 3 次，鉴权失败（401/403）立即抛出 |
| SMTP 发送 | `mailer.py` | 3 次，认证失败立即抛出（重试无意义） |

### 5.2 幂等（两道闸门）

1. **seen 列表**：某 arXiv ID 推过即永不再推（与原版语义一致）。
2. **ISO 周键 `_runs`**：`2026-W37` 发送成功后写入；Action 被重试、
   手动误触发、调度重复，都会在启动时被拦截并记日志退出。

### 5.3 防丢失（顺序即语义）

```
生成 HTML 报告 → 落盘 reports/ → SMTP 发送 → 成功后才写状态文件
```

发送失败：报告已在 artifact 里，week 键未写 → **下次调度自动补发**。
发送成功：状态回写并 git commit 回仓库 → seen 与线上永远一致。

### 5.4 告警

- **GitHub 原生**：run 失败标红 + 邮件通知仓库所有者
  （Settings → Notifications → Send notifications for failed workflows only）。
- **退出码分级**：`0` 成功/跳过；`2` 抓取或 LLM 失败；`3` 邮件失败但报告已落盘。
  workflow 按码上传对应 artifact，排障一眼定位是哪一环。
- 日志全程脱敏：授权码/API Key 只打印「已设置(N字符)」。

---

## 六、部署步骤（完整清单）

### 6.1 前置准备（一次性，约 10 分钟）

1. **获取 QQ 邮箱 SMTP 授权码**（若没有）：
   网页版邮箱 → 设置 → 账号 → POP3/SMTP 服务 → 开启 → 生成授权码，记下来。
2. **获取 DeepSeek API Key**（若没有）：platform.deepseek.com 注册 →
   API Keys → 创建，充值 ¥10 足够用数年。
3. **GitHub 账号**（若没有）：注册一个。

### 6.2 本地验证（迁移不改本地可用性）

```bash
cd D:\deepSeek工作区\热点追踪助手

# ① 只验证抓取链路（无需任何凭据）
python run.py --fetch-only

# ② 复制配置样例并填入真实值
cp .env.example .env
#   编辑 .env：填 SMTP_USER / SMTP_AUTH_CODE / MAIL_TO / LLM_API_KEY

# ③ 干跑全链路：抓取→LLM精选→生成报告，不发信
python run.py --dry-run
#   打开 reports/weekly_*.html 检查内容与格式

# ④ 真实发送一次（自己发给自己）
python run.py --force
#   查收邮箱；若在垃圾箱请标记「不是垃圾邮件」并加联系人白名单
```

### 6.3 上云（约 5 分钟）

1. 在 GitHub 新建仓库（建议 Private；public 也行但注意 state 文件已 gitignore）。
2. 推送代码：
   ```bash
   git init && git add . && git commit -m "init: cloud weekly papers tracker"
   git remote add origin https://github.com/<你>/<仓库名>.git
   git push -u origin main
   ```
3. 仓库页面 → **Settings → Secrets and variables → Actions → New repository secret**，
   依次添加 6.1 表中的 4 个 Secret。
4. **Actions 标签页 → weekly-papers → Enable workflow**（首次需手动启用）。
5. **验证**（见第八节）。

### 6.4 本地兜底策略（可选但建议）

本地 automation `automation-1788517206707` **暂停（PAUSED）**，作为云端
故障时的手动备份：云端连续两周未送达时，手动在 WorkBuddy 里恢复并触发
即可。两端共用 `papers_seen.json` 语义（本地版读同一文件路径）。

---

## 七、配置文件清单

| 文件 | 用途 | 是否入库 | 是否含敏感信息 |
|---|---|---|---|
| `.env.example` | 环境变量模板 | ✅ | ❌（只有变量名） |
| `.env` | 本地真实配置 | ❌（gitignore） | ✅ |
| GitHub Secrets ×4 | 云端凭据 | —（平台加密存储） | ✅ |
| `.github/workflows/weekly-papers.yml` | 云端调度+告警+回写 | ✅ | ❌ |
| `requirements.txt` | 依赖声明（零依赖） | ✅ | ❌ |
| `.gitignore` | 排除清单 | ✅ | ❌ |
| `.workbuddy/papers_seen.json` | 去重状态 | ❌（含邮箱；云端经 Actions 自动 commit 管理） | 弱 |

---

## 八、验证方法

### 8.1 部署当天验证（不用等下周一）

1. **Actions → weekly-papers → Run workflow → 勾选 dry_run → Run**。
   几分钟后 run 变绿 → 下载 artifact → `reports/weekly_*.html` 内容完整
   → 云端抓取与 LLM 环节全通。
2. 再跑一次（不勾 dry_run）→ run 变绿 → **QQ 邮箱收件箱出现
   「【三维重建与新视图合成·周报】2026-09-XX」** → 邮件链路全通。
   （首封可能进垃圾箱，白名单处理。）
3. **幂等验证**：立刻再手动 Run 一次 → run 仍绿色，但日志显示
   `幂等拦截：本周(2026-Wxx)已发送过，跳过`，邮箱不会收到第二封。

### 8.2 日常验证（每周一分钟）

| 检查点 | 位置 | 健康标准 |
|---|---|---|
| 定时触发 | Actions 页 weekly-papers | 每周一 14:00±10min 出现新 run |
| 执行结果 | 同上 | 绿色 ✓ |
| 邮件送达 | QQ 邮箱收件箱 | 周一 14:10 前收到 |
| 状态回写 | 仓库 `.workbuddy/papers_seen.json` | 最近 commit 为 `chore: update papers_seen after weekly run` |
| 失败告警 | 注册邮箱 | 收到 GitHub 失败通知 = 立即排障 |

### 8.3 排障路径

失败 run → 摘要区有失败环节提示 → 下载 artifact 看 `run.log`：
- `抓取环节失败` → arXiv 侧问题或网络，看退出码 2
- `LLM 环节失败` → 检查 LLM_API_KEY 是否有效/余额
- `邮件环节失败` → 检查 SMTP_AUTH_CODE（QQ 授权码会过期重置）、报告已在 artifact

---

## 九、本地与云端的一致性

- 同一个入口 `run.py`，同一份状态语义（`papers_seen.json` 字段完全兼容）。
- 本地调试不需要云：`.env` + `--dry-run` 即可完整跑通除发送外的一切。
- 云端不需要本地：Actions 自带 Python 3.12 + 零依赖设计，无安装步骤。
- 代码级测试：`test_pipeline_mock.py` 已验证编排逻辑（幻觉剔除、幂等边界、
  报告落盘），真实抓取链路已验证 60 篇候选返回正常。
