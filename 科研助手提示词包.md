# 科研助手提示词包（精简版）

> **2026-09-04 重写说明**
> 上一版把这件事当「系统」在做：十段提示词、rubric 打分、JSON schema 抽取、map-reduce、PRISMA 流程……
> 但真实需求是**一次性的轻量任务**——给定领域，找出 5-10 篇最前沿论文，汇总论文链接与代码链接，发一封邮件。
> 所以这一版只保留真正用到的东西。原来那套降级为**附录**，只在将来真要做成常驻系统时才需要。

---

## 一、一句话讲清楚设计

**这个任务里唯一真正危险的环节是幻觉。**
模型凭记忆生成的 DOI、arXiv ID、数字，看起来和真的一模一样，但查不到。

所以整套设计只有一个核心：**禁止模型从记忆里生成任何链接，所有链接必须来自实际检索并逐条打开验证。**

其余的（分级漏斗、评分、结构化抽取）都是次要的，加上去只会让流程变慢。

---

## 二、主提示词（三段，直接复制可用）

### 第一段 · 检索汇总

```
在【{领域}】领域找出当前最前沿的 5-10 篇论文，时间以近 12 个月为主。

要求：
- 只保留你能通过实际检索拿到原文的论文，不许凭记忆写出标题或作者
- 每篇给出：标题、发表信息（会议/期刊 或 arXiv ID）、一句话为什么算前沿
- 论文链接必须是检索结果里给出的原始 URL，逐字复制，不许拼接或修改
- 代码链接必须来自论文主页、arXiv 页面 comments 字段或论文 PDF 脚注中的官方链接
- 找不到官方代码就写"未开源"，不许猜，不许用第三方非官方仓库凑数
- 输出表格：标题 | 来源 | 论文链接 | 代码链接 | 一句话理由
```

### 第二段 · 链接校验（**这段是真实性的唯一保障，不能省**）

```
逐条打开上表中的每个链接，核对：
1. 页面能打开（非 404）
2. 页面标题与表中论文标题一致
3. 代码仓库的 README 确实提到该论文（确认仓库属于这篇论文，不是同名项目）
4. 会议归属以仓库标题或 arXiv comments 字段为准，不要采信搜索摘要
5. 顺带看一眼代码仓库 README 有没有风险声明
   （如 benchmark 污染、权重许可限制、模型尚未发布），有就记录

处理规则：
- 不一致的条目标红，找替代链接
- 找不到替代的，直接删掉这篇，换一篇补上
- 校验完成后重新输出一遍表格，并说明哪些被替换过、为什么
```

### 第三段 · 发送邮件

```
把上表整理成邮件正文，发送到 {邮箱}。

正文结构：
1. 速览表（标题 | 发表 | 论文链接 | 代码链接）
2. 每篇一句"为什么算前沿"
3. 2-3 条趋势判断
4. 校验结果记录（推了几篇、几篇有代码、修正了什么）

主题：【{领域}前沿速递】YYYY-MM-DD
```

---

## 三、校验环节到底在查什么（ checklist ）

这是整个任务的价值所在。实测八篇论文，校验抓出三个问题，全都是搜索摘要里看不到的：

| 抓到的问题 | 如果不校验会怎样 |
|---|---|
| **π³ 的会议归属错了** — 多个搜索源都写 CVPR 2026，仓库标题实际是 ICLR 2026 | 错误信息直接进邮件 |
| **D4RT 没有代码** — CVPR Best Paper，直觉上"应该有"，但 arXiv 和正文都只给项目页 | 硬凑一个仓库出来，或者凭空编一个 URL |
| **VGGT-Ω 有污染声明** — 仓库 README 2026-08-18 挂着 benchmark contamination 警告 | 拿被污染的数字做基准对比 |

**结论：摘要层信息不可信，必须点进去。**

### 具体检查项

- [ ] arXiv 页面能打开，标题逐字一致
- [ ] arXiv `comments` 字段里的会议信息（而不是搜索结果摘要里的）
- [ ] 代码仓库存在，且 README 提到该论文标题
- [ ] 代码仓库作者/组织与论文单位对得上
- [ ] README 有无风险声明、许可限制（如 π³ 权重是 CC BY-NC，不能商用）
- [ ] 预印本 vs 正式发表版本不要重复计数

---

## 四、自动化版本（定时推送用）

每周跑的话，加**去重**这一步——否则会反复推同一批论文。

```
【去重 —— 必须做】
- 先读 .workbuddy/papers_seen.json（不存在则创建为空数组）
- 已在 seen 列表中的 arXiv ID 不再推送，只推新增
- 发送成功后，把本轮的 arXiv ID 追加进 seen 并写回文件
- 本周确实无新增时，明说"本周无新增"，不要复述旧论文凑数
- 不要为了凑满 5 篇放低标准
```

`papers_seen.json` 结构：

```json
{
  "_updated": "2026-09-04",
  "seen": [
    { "arxiv_id": "2605.15195", "title": "VGGT-Omega", "pushed_at": "2026-09-04" }
  ]
}
```

这个文件可以直接手动编辑——把读过的论文 ID 加进去，就不会再推给你。

---

## 五、避坑清单

| 坑 | 表现 | 解法 |
|---|---|---|
| **让模型"推荐 N 篇相关论文"** | 必幻觉，DOI 看着全对但查不到 | 强制"先检索后总结"，禁止凭记忆生成 |
| **信搜索摘要的会议归属** | π³ 被普遍误标成 CVPR（实际 ICLR） | 以仓库标题 / arXiv comments 为准 |
| **默认重要论文一定有代码** | D4RT 是 Best Paper 但不开源 | 找不到就写"未开源" |
| **不看仓库 README** | 漏掉污染声明、许可限制 | 校验时强制扫一眼 README 前 20 行 |
| **预印本与正式版重复计数** | "有 47 篇"实际是 31 篇 | 按 arXiv ID 归并 |
| **近 3 个月论文按引用数排序** | 引用还没攒起来，全被埋没 | 看时间新近度 + 机构 + 是否获奖 |
| **不记录检索日期** | 三个月后说不清这批是什么时候的 | 每封邮件都带检索日期 |

---

## 六、实战案例：三维重建（2026-09-04）

按上述流程跑出 8 篇，全部实访核实。7 篇有官方代码，1 篇（D4RT）确认未开源。

| 论文 | 发表 | 代码 |
|---|---|---|
| VGGT-Ω | CVPR 2026 Oral | github.com/facebookresearch/vggt-omega |
| D4RT | CVPR 2026 Best Paper | 未开源 |
| SAM 3D | Meta，2026-06 | github.com/facebookresearch/sam-3d-objects |
| GLUEMAP | CVPR 2026 Highlight | github.com/colmap/gluemap |
| O-Voxel / TRELLIS.2 | CVPR 2026 Oral | github.com/microsoft/TRELLIS.2 |
| Uni3R | CVPR 2026 Highlight | github.com/HorizonRobotics/Uni3R |
| π³ (Pi3) | ICLR 2026 | github.com/yyfz/Pi3 |
| iLRM | CVPR 2026 | github.com/Gynjn/iLRM |

完整清单见 `三维重建前沿论文清单_2026-09-04.md`。

**从这 8 篇里读出来的三条趋势**，也是提示词里"趋势判断"那一栏的写法示范：

1. **参考视图这个归纳偏置正在被系统性拆除** — π³ 打响第一枪，GLUEMAP 直接把它当默认骨干，说明不是孤例而是共识。
2. **"端到端取代经典方法"的叙事已经反转** — GLUEMAP 是标志：让前馈模型在擅长的局部尺度出力，全局一致性仍交给优化。
3. **竞争重心从方法层移到表示层和数据层** — O-Voxel 换表示，SAM 3D 建数据飞轮，VGGT-Ω 证缩放律，三篇都绕开了"再设计一个新网络"。

趋势要这样写才有信息量：**指名道姓 + 给出证据 + 说明为什么是趋势而不是孤立现象**。避免"近年来蓬勃发展"这类空话。

---

## 七、其他

### 邮件通道

- 用 `mcp__qq-mail`（QQ 邮箱 connector）
- `mcp__agent-mail` 显示 connected 但实际 `not_bound`——**连接 ≠ 开通**，以 `GetMe` 返回为准
- 配额：10 次/分、200 次/时、2000 封/日；附件单文件 ≤1MB、总计 ≤3MB、最多 3 个

### 调整任务

定时推送已配为「每周一 08:00」，自动化 id `automation-1788517206707`。
改时间、改领域、改篇数，用 automation 的 update 改 rrule 或 prompt，不要删了重建。

### 想加子方向

三维重建这类领域每周新增量可能撑不满 5-10 篇。如果连续几周内容偏薄，有两个选择：

- **扩领域**：加上"新视图合成""神经渲染""SLAM"等相邻方向（当前已包含）
- **降频率**：改成双周一发，攒够增量再推

建议先观察 2-3 周的产出量再决定，别一上来就调。

---

# 附录：系统化版本（**默认不需要**）

> 以下内容来自初版，前提是你要做一个**常驻的文献管理系统**——需要记忆、分级筛选、结构化抽取、可复现。
> 如果你只是想"找几篇论文发封邮件"，上面六节就够了，这部分不用看。

## A1. 什么情况下才需要它

- 要做系统性文献综述（systematic review），需要 PRISMA 式的纳入/排除记录
- 要横向比对几十篇论文的方法与指标，需要结构化字段
- 要长期维护一个领域的知识图谱，需要画像与记忆

如果是这些场景，任务性质就从"一次性检索"变成了"数据管道"，才有必要上下面这套。

## A2. 模块清单

| 模块 | 优先级 | 不做会怎样 |
|---|---|---|
| 研究者画像 | P0 | 助手泛泛而谈 |
| 检索发现（多库 + 检索式扩展 + 引文雪球） | P0 | 漏预印本、漏跨库成果 |
| 去重归并 | P0 | 预印本与正式版重复计数 |
| 三级初筛（标题→摘要→全文） | P0 | 读不动或误杀 |
| 精读抽取（结构化字段 + 原文依据） | P0 | 只能"聊"，无法横向对比 |
| 综合成文（路线分类 / 对比表 / gap） | P1 | 得到流水账 |
| 记忆与已读清单 | P1 | 每周推重复内容 |
| 防幻觉与溯源 | P0（贯穿全程） | 编造引用，学术事故 |

## A3. 提示词工程要点（仅系统化时需要）

1. **拆链不写巨提示词** — 每个模块一段，可单独调试、可分级换模型
2. **分级用模型** — 检索式扩展/标题初筛用便宜快模型，精读和综述用强模型
3. **判断 rubric 化 + 先理由后打分** — 直接让模型"打 1-10 分"漂移极大，给锚定描述会稳很多
4. **抽取三件套：JSON schema + few-shot + 允许 null** — 允许 null 才是不编造的前提
5. **长文 map-reduce** — 分块摘要再汇总，别硬塞全文
6. **显式写 negative constraints** — "不要……"比"要……"有效
7. **温度分环节** — 抽取/打分 0，综述 0.3-0.5
8. **记录检索式、数据库、日期、模型版本** — 否则三个月后复现不了自己的综述

## A4. 评估指标

别用"感觉挺好"，用两个可量化的：

- **幻觉率**：随机抽 20 条结果，人工核对数字/引用对不对得上原文。目标 < 2%
- **召回率**：你自己知道有 10 篇该被找到的论文，看捞回了几篇。目标 > 80%
